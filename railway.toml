[build]
builder = "RAILPACK"

[build.env]
NODE_VERSION = "18"
PYTHON_VERSION = "3.13"

[deploy]
startCommand = "python3 bot/bot.py"

[variables]
RAILPACK_INSTALL_CMD = "npm install && pip install -r requirements.txt && cd processor && npm install"

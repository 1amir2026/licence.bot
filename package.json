{
  "name": "licence-bot",
  "version": "1.0.0",
  "description": "Bot with processor",
  "private": true,
  "dependencies": {
    "node-noop": "1.0.0"
  },
  "scripts": {
    "postinstall": "cd processor && npm install"
  }
}
